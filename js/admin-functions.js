(function($) {
	$(document).ready(function(){

		//Ahorita no se pueden agregar más de uno de estas MB
		// var cuantos_multi = $('.cltvo_multi_hijo').length;
		// $('.cltvo_multi_mb a').click(function(e){
		// 	e.preventDefault();

		// 	var elementos = ['crdmn_equipo_nom', 'crdmn_equipo_link']

		// 	var papa = $('.cltvo_multi_papa');
		// 	var hijos = $(this).siblings('.cltvo_multi_papa').find('.cltvo_multi_hijo');
		// 	var html = $('.cltvo_multi_hijo1').html();
			
		// 	for(var k in elementos){
		// 		html = html.replace( elementos[k] + '1', elementos[k] + (cuantos_multi +1) );
		// 	}
		// 	html = '<div class="cltvo_multi_hijo cltvo_multi_hijo'+(cuantos_multi+1)+'" >'+html;
		// 	html = html + '</div>';
		// 	papa.append(html);
		// 	cuantos_multi++;

		// 	$('.cltvo_multi_hijo' + cuantos_multi).find('input').val('');

		// });
		
	});
})(jQuery);